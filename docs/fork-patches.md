# Curated fork patch registry

This ledger is the fork-maintenance tax that the fleet repository is meant to end; see [`docs/admiralty-fleet-repo.md`](admiralty-fleet-repo.md) for what replaces it and how far that work has got.
Until a vessel is actually cut over, this registry stays authoritative.
The 2026-08-01 review, extended on 2026-08-02, records all 94 fork-only, non-merge commits reported by `git rev-list --oneline --no-merges "$upstream..$fork"` for fork `22b6464` against canonical upstream `a805766`.
That fork commit is a pinned snapshot of the fork's default branch, not a live reference.
`bin/fm-fork-sync-check.sh` resolves its own fork side from the live origin `HEAD` instead, so its commit set grows the moment anything lands on the fork's default branch while this pin stays where it is.
A divergence between the check's count and this registry's row count is therefore expected and is not a defect in either.
`Freudator86/firstmate` is a shared repository into which other vessels also push, so part of such a divergence is their work rather than an omission on this home's side.
The remedy is to add one row per unrecorded commit and re-stamp the pin in the sentence above; `git rev-list --oneline --no-merges <pinned-fork>..origin/main` names exactly what a stale pin is missing.
The registry is deliberately not self-updating, because a generated row could only restate the diff, which is the reconstructed motive this document exists to prevent.

Each fork-only, non-merge commit has one row recording what it does, the problem that required it, its current verdict, and whether another vessel would want it.
The Why column uses the associated pull request's `## Intent` section as its primary source.
Where the surviving repository has no matching pull request or no usable intent, the row says `Warum nicht mehr feststellbar` instead of reconstructing a motive from the diff.

Verdict answers whether this vessel still needs the patch when it moves.
Use `absorbed` when upstream fully replaces the patch and it can be dropped, `keep` when the patch must travel with the vessel, and `upstream-candidate` when it must travel and should additionally be proposed upstream.
The matching `git cherry "$upstream" "$fork"` check found no patch-equivalent commit mechanically, so no `absorbed` verdict can rest on it.
The test used instead is whether upstream already carries the whole of what the commit contributes, judged at content level rather than by filename.
A shared path proves nothing on its own, because a commit can edit a file upstream also has while every line it changes is fork-authored, and a commit that mixes an already-upstream effect with fork-only material is not absorbed however clearly the upstream side is evidenced.
That is also why the check's own per-commit `absorbed` label in `state/fork-sync.pending` is a prefilter and never this column's verdict: it fires on patch equivalence or on the fork and upstream tips agreeing across the commit's touched files, which is exactly the filename-level shortcut rejected here.
Every `absorbed` row therefore names the upstream file, rule or provenance that already carries its effect, and every row denied that verdict names what upstream does not have.
That rule is why a patch and its own guard can end up on opposite sides: `dba4fdb` restores a skill upstream still ships, while `779c347` and `33f2e8e` pin that skill inside `tests/fm-instruction-owners.test.sh`, of which upstream has no copy.
It is why `93b21ac`, `fc25d2f` and `063e03e` are not absorbed despite carrying real upstream substance - each creates a file upstream does not have, and dropping the commit would drop that file with it.
And it is why `1c53ff8` is not absorbed either, reaching the same collision through a shared path rather than a new one: it edits `docs/configuration.md`, which upstream has, but the paragraph it edits is fork-authored throughout.

Scope answers a separate question and is assigned independently of Verdict: would another vessel hit this problem and want this behavior, or does the patch exist only because of this home's own configuration and history?
A `keep` patch can still be general - the fork-sync checks are the clearest case, since every downstream vessel wants them while upstream, being the upstream, has no use for them.
An `absorbed` patch can still be ship-specific, as `dba4fdb` is: the skill it restores belongs to upstream, but the loss it repairs was this fork's alone.
The honest result is lopsided, with only eleven ship-specific rows, because these patches overwhelmingly harden mechanisms every vessel shares; what is genuinely local to this home is its Codex approval profile (`2bdd0f1`), the two repairs to its own history reconciliation (`abcc1e1`, `e59d3a0`), its admiralty move record (`356cd52`), the restoration of a skill only this fork had lost (`dba4fdb`), and the six commits that build and correct this registry itself (`78c6ad1`, `26aef70`, `229f396`, `06d7d54`, `baed08b`, `c839e29`).

Re-review a row when the upstream tip moves.

| Commit | What it does | Why it exists | Verdict | Scope | Last reviewed upstream |
| --- | --- | --- | --- | --- | --- |
| `e623032` | Adds the AXI-suite updater and primary safety guards. | Warum nicht mehr feststellbar; the current repository's PR #1 describes a different post-repoint patch. | keep | general | `a805766` |
| `4939c5b` | Wakes supervision on Bridge inbox traffic. | Warum nicht mehr feststellbar; the current repository's PR #2 describes a different post-repoint patch. | keep | general | `a805766` |
| `df159d2` | Adds the upstream Firstmate update check. | Warum nicht mehr feststellbar; the current repository's PR #3 is closed and unmerged, and its body carries the later history-reconciliation intent rather than this patch's original review. | keep | general | `a805766` |
| `8f3190b` | Reads Bridge inbox state from fetched `origin/main`. | Warum nicht mehr feststellbar; the current repository's PR #4 is the later history-recovery merge rather than this patch's original review. | keep | general | `a805766` |
| `c8496fb` | Adds curated-fork sync detection. | Warum nicht mehr feststellbar; no associated pull request survives in the current repository. | keep | general | `a805766` |
| `a3794c7` | Removes an unused local fetch and de-personalizes the fork-sync cron documentation. | Warum nicht mehr feststellbar; no associated pull request survives in the current repository. | keep | general | `a805766` |
| `40b1f11` | Installs AFK signal traps before lock acquisition. | Warum nicht mehr feststellbar; no associated pull request survives in the current repository. | upstream-candidate | general | `a805766` |
| `6aedfec` | Documents fork-sync in architecture and configuration references. | Warum nicht mehr feststellbar; no associated pull request survives in the current repository. | keep | general | `a805766` |
| `9053fc4` | Scopes the Bridge inbox vessel per home. | Warum nicht mehr feststellbar; the current repository's PR #6 describes a different post-repoint patch. | keep | general | `a805766` |
| `8a5fd11` | Deduplicates Bridge inbox wakes by surfaced signature. | Warum nicht mehr feststellbar; the current repository's PR #7 describes a different post-repoint patch. | keep | general | `a805766` |
| `2bdd0f1` | Configures the Codex Graphify approval profile and hooks. | Codex lacked this home's tracked approval and Graphify hook profile, so its sessions could not apply the fleet's required safety and knowledge hooks consistently. | keep | ship-specific | `a805766` |
| `b384c1b` | Skips non-repository directories nested below the project root during fleet sync. | Enclosing-repository discovery misclassified orphan worktree-container directories as projects and emitted bogus diverged or stuck results. | upstream-candidate | general | `a805766` |
| `abcc1e1` | Restores AXI, Bridge, script-index, and guard-test content dropped during history reconciliation. | The genuine merge of diverged fork and origin histories had to preserve recovered fleet features while retaining the newer condensed instruction surface. | keep | ship-specific | `a805766` |
| `e59d3a0` | Corrects Bridge vessel configuration precedence in `AGENTS.md`. | The hand-resolved history reconciliation could combine newer instructions and recovered Bridge behavior into a semantically wrong contract even when the textual merge succeeded. | keep | ship-specific | `a805766` |
| `b0b04d0` | Detects drift between the primary checkout and its own origin. | Project fleet sync and upstream-template checks did not cover the primary Firstmate checkout itself, leaving ahead, behind, and diverged states invisible. | upstream-candidate | general | `a805766` |
| `e3ad719` | Hardens test cleanup and wake-queue locking against temporary-file leaks and filesystem failures. | Repeated tests could exhaust `/tmp`, while quota and filesystem errors were mistaken for lock contention and stale-lock recovery could recurse without a hard bound. | upstream-candidate | general | `a805766` |
| `a8ce717` | Arms the direct Telegram receiver during session start. | Homes with `config/telegram.env` required a manual receiver start on every session and could therefore miss the normal tracked supervision lifecycle. | keep | general | `a805766` |
| `33176e1` | Adds an explicit resting state for idle secondmates. | Registered secondmates with no work still forced Codex-style turn-end supervision checkpoints despite the contract that an empty queue is healthy. | upstream-candidate | general | `a805766` |
| `761c18e` | Bounds wake-lock acquisition and stale-lock recursion. | Persistent contention or filesystem failure could make production lock acquisition wait forever or recurse through unbounded `.steal` chains. | upstream-candidate | general | `a805766` |
| `2c6ab08` | Reaps watcher processes owned by a dead arm wrapper. | A validation watcher started from `.no-mistakes/worktrees` could outlive its wrapper and remain as an orphan without weakening singleton or liveness behavior. | upstream-candidate | general | `a805766` |
| `33bc721` | Makes benign supervision re-arms quiet and parks terminal human waits. | One crew event could cost repeated drain-and-rearm turns, while terminal tasks waiting only on human action kept generating stale supervision noise. | upstream-candidate | general | `a805766` |
| `c5063d8` | Isolates the Bridge inbox test from ambient `FM_BRIDGE_ROOT`. | A shell-exported live Bridge root redirected the positive-wake fixture to a nonexistent checkout and left its unbounded `wait` hanging indefinitely. | keep | general | `a805766` |
| `4d1fd1c` | Adds fleet-wide transcript-safe secrets doctrine. | Secret files, credential values, and unfiltered environments could recur in transcripts because safe handling was not a shared invariant for every agent type. | upstream-candidate | general | `a805766` |
| `db06c4b` | Guards a Pi TypeScript-import test on runtimes without native support. | One lock-ownership test missed the established Node capability guard and broke the suite on supported hosts without native `.ts` imports. | upstream-candidate | general | `a805766` |
| `117dec5` | Restores Pi successor, retry, and single-flight wake continuity. | The fork lacked upstream's recovery path and its timing-sensitive test could hang or undercount successor attempts on slower login shells; upstream's `.pi/extensions/fm-primary-pi-watch.ts` carries the same successor, retry and single-flight logic as the fork's, so upstream still holds the recovery path this commit restores. | absorbed | general | `a805766` |
| `b918595` | Adds a guarded direct relay for Bridge envelopes. | Firstmate could not publish existing Bridge send, inbox, status, or broadcast envelopes without routing the operation through a project ship pipeline. | keep | general | `a805766` |
| `b9cb8bf` | Moves watcher supervision into a per-home systemd service with a fallback keeper. | Harness-owned background watchers died with harness lifecycles and lacked durable health, repair, and zero-wake-loss semantics. | upstream-candidate | general | `a805766` |
| `c6feb47` | Watches a space-separated list of Bridge vessel inboxes. | StaffCaptain needed to drain both its own inbox and `inbox/captain`, while the existing configuration accepted exactly one vessel. | keep | general | `a805766` |
| `063e03e` | Hardens Pi operational-input classification and expands supervision tooling. | Public marker text was being treated as trusted operational input, so typed or repeated user content could be hidden or misclassified; the absorption evidence is genuinely mixed, because `bin/fm-operational-input.sh` and `tests/fm-operational-input.test.sh` carry that stated substance and exist upstream, while the same 143-file commit creates seven files upstream does not have at all - `bin/fm-continuity-command-policy.mjs`, `bin/fm-continuity-pretool-check.sh`, `tests/fm-claude-continuity-live-e2e.test.sh`, `tests/fm-continuity-pretool-check.test.sh`, `tests/fm-install-herdr.test.sh`, `tests/fm-nm-test-contract.test.sh` and `tests/no-mistakes-required-workflow.test.sh` - so dropping it as absorbed would take the supervision tooling with it. | upstream-candidate | general | `a805766` |
| `7ce70cd` | Adds a five-second Bridge frequency monitor. | The roughly five-minute watcher fallback made live Bridge inbox notice too slow, while tightening the agentic supervision loop would have added turns and coupling. | keep | general | `a805766` |
| `7e6ec22` | Follows GitLab merge requests through merge completion. | Warum nicht mehr feststellbar in this fork; the numbered pull request belongs to upstream, whose `bin/fm-pr-lib.sh` already carries the same merge-request handling. | absorbed | general | `a805766` |
| `052df46` | Groups projected Herdr children beneath their owning parents. | Warum nicht mehr feststellbar in this fork; the numbered pull request belongs to upstream, and every file this commit changes exists upstream. | absorbed | general | `a805766` |
| `9e6e895` | Excludes operational injections from Ahoy message boundaries. | Warum nicht mehr feststellbar in this fork; the numbered pull request belongs to upstream, and every file this commit changes exists upstream. | absorbed | general | `a805766` |
| `578d9c5` | Classifies operational inputs canonically across harnesses. | Warum nicht mehr feststellbar in this fork; the numbered pull request belongs to upstream, and every file this commit changes exists upstream. | absorbed | general | `a805766` |
| `1eb46a3` | Reconciles bootstrap ordering and watcher close-classification documentation. | The fork-sync PR needed code and continuity documentation to agree after the upstream history reconciliation. | upstream-candidate | general | `a805766` |
| `3211115` | Adjusts arm-policy and shared-test code during lint stabilization. | Warum nicht mehr feststellbar; the commit has no distinct intent beyond the surrounding validation run. | upstream-candidate | general | `a805766` |
| `92e08d0` | Applies CI fixes to backend and watcher-lock tests. | Warum nicht mehr feststellbar; the commit has no distinct intent beyond the surrounding validation run. | upstream-candidate | general | `a805766` |
| `b4c7699` | Applies a further CI fix to the watcher-lock test. | Warum nicht mehr feststellbar; the commit has no distinct intent beyond the surrounding validation run. | upstream-candidate | general | `a805766` |
| `36edd37` | Applies the final CI fix to the watcher-lock test. | Warum nicht mehr feststellbar; the commit has no distinct intent beyond the surrounding validation run. | upstream-candidate | general | `a805766` |
| `a1ab5b5` | Corrects a stale away-marker classification note. | The fork-sync PR had to leave architecture documentation accurate after reconciling upstream behavior and the fork's retained changes. | upstream-candidate | general | `a805766` |
| `6f62c6b` | Hardens stale-lock concurrency tests and fixes temporary-root variable shadowing. | Heavy CI load made the stale-lock assertion nondeterministic, and shadowed temporary-root state could drop cleanup while syncing the fork with upstream. | upstream-candidate | general | `a805766` |
| `5baa843` | Makes fork-sync drift detection content-aware. | Rebased, squashed, or byte-identical upstream changes were reported as outstanding solely because their commit identities differed. | keep | general | `a805766` |
| `5229506` | Writes Claude's task hook to a separate settings file and loads it explicitly. | Spawn truncated tracked `.claude/settings.local.json`, and the resulting tracked dirtiness then made safe teardown refuse forever. | upstream-candidate | general | `a805766` |
| `1142961` | Collapses duplicate crew-event wakes and rechecks active validation runs. | Status and turn-ended files produced duplicate cycles, and stale verdicts escalated long-running no-mistakes work even when the agent was demonstrably active. | upstream-candidate | general | `a805766` |
| `e743665` | Resolves update and fork-sync comparison bases from per-home configuration. | A hardcoded canonical URL made every non-direct deployment compare against the wrong repository and report meaningless currency. | upstream-candidate | general | `a805766` |
| `e52cc76` | Selects a fleet role through per-home configuration and tracked overlays. | Separate code forks for roles were unnecessary, while one shared code root lacked a durable role selector, delivery path, and coordinator dispatch refusal. | upstream-candidate | general | `a805766` |
| `ce43899` | Re-verifies Codex adapter facts against codex-cli 0.145.0. | The recorded Codex behavior lagged the binary used by this vessel, and the load-bearing busy signature and interruption behavior could not safely rest on recollection. | upstream-candidate | general | `a805766` |
| `356cd52` | Records the admiralty repository, pin, ancestry, and ownership contract. | Without a durable record, later sessions would have to reconstruct why admiralty descends from the fork and how its vendored and fleet-owned paths are partitioned. | keep | ship-specific | `a805766` |
| `a0fe90d` | Ignores checkout-local harness runtime artifacts in fresh clones. | Clone-private excludes did not travel with a vessel, so normal harness artifacts made fresh checkouts dirty and silently stopped self-update. | upstream-candidate | general | `a805766` |
| `a669313` | Accepts resolved captain decisions after archive retention. | The completion gate treated a resolved hold moved from the live backlog into `done-archive.md` as missing and refused cleanup indefinitely. | upstream-candidate | general | `a805766` |
| `967a7f6` | Produces conforming PDFs and verifies them through reader diagnostics. | A 27-page handover PDF reached the captain unreadable because `pdfunite` emitted an invalid trailer and Ghostscript still returned exit code zero for that defect. | upstream-candidate | general | `a805766` |
| `6bc798a` | Adds a repeatable two-analyst-and-judge model panel. | Hand-built panels were costly and fragile, while independent analysts plus an adversarial judge had exposed factual disagreement, shared mistakes, and hidden captain decisions. | upstream-candidate | general | `a805766` |
| `ce8309f` | Requires a live away pusher before an away turn may end. | The presence of `state/.afk` falsely proved delivery coverage even when the daemon that should deliver queued wakes was dead. | upstream-candidate | general | `a805766` |
| `050cdce` | Lints stale backlog dependency edges without mutating them. | Missing, already-done, or archive-disagreed dependency targets could rot durably and keep queue state misleading without any startup diagnostic. | upstream-candidate | general | `a805766` |
| `8502757` | Isolates AXI-suite installations under each vessel's `FM_HOME`. | A shared global npm prefix let independently updating homes race and write across vessel boundaries. | keep | general | `a805766` |
| `7dd7be5` | Feeds growing fleet-snapshot JSON to `jq` over stdin. | `--argjson` placed unbounded home data in one argv string, so large backlogs and secondmate inventories exceeded `MAX_ARG_STRLEN` and produced no snapshot. | upstream-candidate | general | `a805766` |
| `bbf0023` | Serves Lavish boards on each vessel's tailnet address and allocated port. | Loopback links were unusable on the captain's PC or phone, and multiple vessels on one host collided on Lavish's fixed default port. | keep | general | `a805766` |
| `93b21ac` | Ignores all captain-private `config/` material by pattern. | Name-by-name ignores failed open for every new private config file and could let a plain `git add -A` stage a Telegram bot token; upstream's `.gitignore` has ignored `config/` since its initial commit of 2026-06-11 and refined the rule on 2026-06-29, both before this fork commit of 2026-07-31, so the ignore rule itself is already upstream, but the same commit creates `tests/fm-private-material-ignore.test.sh` with 121 lines and upstream has no such file, so the patch cannot be dropped wholesale. | upstream-candidate | general | `a805766` |
| `fc25d2f` | Narrows the ignore comment, maps the change to tests, and guards rule breadth. | The private-config fix needed behavioral coverage that prevented both future enumeration drift and accidental over-broad ignores; its `.gitignore` and `docs/configuration.md` edits refine a rule upstream's own `.gitignore` already carries, but it adds 41 further lines to `tests/fm-private-material-ignore.test.sh` plus the `bin/fm-test-run.sh` mapping that selects it, and upstream has no such test, so the patch cannot be dropped wholesale. | upstream-candidate | general | `a805766` |
| `2d6ff5e` | Makes the ignore-breadth guard fail on Git errors. | A Git probe failure could otherwise be mistaken for proof that an unrelated path remained visible; upstream has no `tests/fm-private-material-ignore.test.sh`, so this guard cannot have been absorbed even though the ignore rule it protects exists upstream. | upstream-candidate | general | `a805766` |
| `feb5141` | Makes role and panel tests assert ignore behavior instead of one literal rule. | Literal-source assertions would reject an equally safe implementation and duplicate the contract instead of testing its effect. | upstream-candidate | general | `a805766` |
| `ab11e91` | Isolates ignore probes and maps every `.gitignore` dependent test. | Private excludes could mask a missing shared rule, while incomplete changed-file mapping could skip the tests that enforce ignore behavior. | upstream-candidate | general | `a805766` |
| `9411e07` | Makes the changed-test map guard enforcing and correctly scoped. | A mapping failure could die in the wrong scope without proving that every `.gitignore` dependent would run. | upstream-candidate | general | `a805766` |
| `8709b19` | Covers seed-marker ignore behavior and test-isolation refusals. | Broadening the shared ignore contract risked silently weakening secondmate marker and isolated-test safety guarantees; upstream has no `tests/fm-test-lib.test.sh`, so this coverage cannot have been absorbed. | upstream-candidate | general | `a805766` |
| `1c53ff8` | Removes a stale suite enumeration from the ignore rationale. | Duplicating the current suite list in prose would immediately drift from the test runner that owns it; upstream carries `docs/configuration.md` but none of the paragraph this commit rewrites, which names `tests/fm-private-material-ignore.test.sh`, the `config/*` rule with its negation escape hatch, and the `.gitignore` arm of the changed-file map in `bin/fm-test-run.sh`, so the file exists upstream while the content does not. | upstream-candidate | general | `a805766` |
| `cfd67ab` | Adds the tracked executor role overlay. | The executor selector was reserved but had no overlay, leaving Tugboat's role boundary undefined and preventing the planned admiralty move. | upstream-candidate | general | `a805766` |
| `357ef6b` | Reconciles executor deployment duties with the project-write boundary. | An executor role could otherwise read as permission to write another vessel's project despite Firstmate's always-loaded prohibition. | upstream-candidate | general | `a805766` |
| `643051b` | Points the executor overlay at hard rule 1 instead of restating it. | A second copy of the project-write contract would drift and make the new role appear to own a safety rule it only applies. | upstream-candidate | general | `a805766` |
| `e94925e` | Updates role tests so executor documentation is no longer treated as missing. | Tests and documentation could preserve the old reserved-but-unwritten state after the overlay became real. | upstream-candidate | general | `a805766` |
| `879bf50` | Adds a shared board layout and the `/decisionboard` capability. | Existing boards recompiled remote Tailwind assets on every open, lacked a standard fast layout, and presented duplicated model-panel decision records without structural collapse. | upstream-candidate | general | `a805766` |
| `f6b585a` | Refuses line-wrapped remote board references and surfaces empty board states. | The no-network guard could miss wrapped URLs, while a structurally valid but empty board could fail silently. | upstream-candidate | general | `a805766` |
| `8544518` | Scans CSS `@import` as a statement. | Treating `@import` as a trailing word left the board's no-network proof vulnerable to CSS syntax the browser would still execute. | upstream-candidate | general | `a805766` |
| `fda5664` | Restricts CSS network scanning to browser-executed contexts. | Scanning inert text as CSS created false alarms without improving the guarantee about actual browser requests. | upstream-candidate | general | `a805766` |
| `1cd246d` | Covers every style-attribute form that can fetch a resource. | Alternate attribute quoting and layout forms could bypass a guard that recognized only one serialization. | upstream-candidate | general | `a805766` |
| `c822045` | Documents table, monospace, distribution, and `data-src` board coverage. | The reusable layout and its no-network boundary needed an explicit owner so later boards would not guess which forms were supported. | upstream-candidate | general | `a805766` |
| `9000261` | Points board guard documentation at `fm-board.sh --help`. | Repeating guard mechanics in the board document would create a second owner and drift from the executable contract. | upstream-candidate | general | `a805766` |
| `b6d84f4` | Adds evidence-based review-quality grading. | The decision to keep no-mistakes relied on the tool's own finding ledger, so false-positive rate and whether fixes actually improved code had no independent measurement. | upstream-candidate | general | `a805766` |
| `dba4fdb` | Restores the `ask-user-authority` skill from upstream. | A squash import lost the procedure even though `AGENTS.md` still required the behavior, leaving ask-user findings without their decision owner; `.agents/skills/ask-user-authority/SKILL.md` exists upstream and the commit message records that it was restored from there. | absorbed | ship-specific | `a805766` |
| `779c347` | Pins the authority skill and its trigger in instruction-owner tests. | The same skill or trigger could otherwise disappear again while every repository test stayed green; upstream has no `tests/fm-instruction-owners.test.sh`, so the pin cannot have been absorbed even though the skill it pins exists upstream. | upstream-candidate | general | `a805766` |
| `7fe9640` | Fixes grading chain boundaries, diff parsing, and metric provenance. | Incorrect fix-chain boundaries and unproven source attribution would make the new independent review metrics misleading. | upstream-candidate | general | `a805766` |
| `33f2e8e` | Pins authority-skill body structure and collapses redundant trigger assertions. | The authority procedure's load-bearing body and trigger could drift silently under the same unguarded-skill failure that caused its loss; upstream has no `tests/fm-instruction-owners.test.sh`, so these assertions cannot have been absorbed. | upstream-candidate | general | `a805766` |
| `a9931ca` | Deduplicates identical fix chains and normalizes detection paths. | One correction could be counted more than once or attributed to a different path spelling, corrupting the review-quality evidence. | upstream-candidate | general | `a805766` |
| `0277ee0` | Rejects bare basenames and sentence-final path tokens in grading evidence. | Loose path matching could treat persuasive prose as proof that a finding actually targeted the measured file. | upstream-candidate | general | `a805766` |
| `832fc66` | Documents grading environment variables and links the first-reading record. | Without the tunable bounds and provenance record, another reviewer could not reproduce or interpret the grading result honestly. | upstream-candidate | general | `a805766` |
| `e58e931` | Requires every skill to declare a load trigger and a conditional frontmatter description. | `ask-user-authority` lost its trigger line, every test stayed green, and the skill silently stopped being loaded for months, so the check enumerates the `.agents/skills/` directory rather than naming skills in a list, because a hand-maintained list carries the identical silent-staleness failure mode as the gap it guards. | upstream-candidate | general | `a805766` |
| `61b03a9` | Anchors the skill frontmatter probes, stamps the audit's counts with their base tree, and links the audit record from the guidelines skill. | Warum nicht mehr feststellbar; the commit has no distinct intent beyond the surrounding validation run. | upstream-candidate | general | `a805766` |
| `7762040` | Anchors the frontmatter name probe and corrects the audit's recorded cost and base stamps. | Warum nicht mehr feststellbar; the commit has no distinct intent beyond the surrounding validation run. | upstream-candidate | general | `a805766` |
| `78ffa4f` | Records the frontmatter description as a second skill arrival route and states what the new check does and does not enforce. | The description is the only arrival path left in a deployment where no instruction-surface trigger line is possible at all, and the check's condition clause is knowingly a weak floor that proves a condition is stated rather than that it is the right one, so the convention's own section had to say that triggers are written precisely rather than written to the check. | upstream-candidate | general | `a805766` |
| `78c6ad1` | Completes this registry so that every fork-only, non-merge commit carries a row. | The registry covered only part of the fork-only set, leaving the remaining commits with no recorded motive and no verdict to consult at cutover. | keep | ship-specific | `a805766` |
| `26aef70` | Corrects registry verdicts and scope assignments and stamps the fork snapshot the review measured. | Several rows carried verdicts and scope the review's own evidence did not support, and the snapshot those counts referred to was unstated, so a later reader could not tell which fork tip the table described. | keep | ship-specific | `a805766` |
| `229f396` | Removes the `absorbed` verdict from rows whose commits mix already-upstream effects with fork-only material. | A commit that also creates or edits material upstream does not have cannot be dropped as absorbed however clearly its upstream side is evidenced, because dropping it would drop that material too. | keep | ship-specific | `a805766` |
| `06d7d54` | Applies the content-level absorption test to the registry's rows. | Filename-level agreement between fork and upstream was standing in for evidence that upstream carries the whole of what a commit contributes. | keep | ship-specific | `a805766` |
| `baed08b` | Records the upstream provenance that already carries `117dec5`'s effect. | The row asserted absorption without naming the upstream file the registry requires of every `absorbed` verdict. | keep | ship-specific | `a805766` |
| `c839e29` | States that the fork-sync check's own `absorbed` label is a prefilter and never this column's verdict. | The check fires on patch equivalence or on fork and upstream tips agreeing across a commit's touched files, which is exactly the filename-level shortcut this registry's absorption test rejects. | keep | ship-specific | `a805766` |
